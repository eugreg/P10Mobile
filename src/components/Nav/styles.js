import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    nav: {
        alignItems: "center",
        height: 50,
        backgroundColor: "#8558",
    },
})

export default styles